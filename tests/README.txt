Datenbankkonfiguration für die PHPUnit-Tests:

Host: localhost
DB:   phpunit_otc (Alle Rechte für die DBs phpunit\_%)
User: phpunit
Pass: phpunit

Kopiere die Tabellen und deren Daten von der "echten" oTranCe DB in die neue DB.