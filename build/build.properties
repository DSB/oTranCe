project.version = 1.0.0
project.distdir = ${project.basedir}/dist
project.builddir = ${project.basedir}/build
project.builddir.temp = ${project.builddir}/temp
project.testsdir = ${project.basedir}/tests

dist.archives.basename = ${project.distdir}/${phing.project.name}-${project.version}
dist.archives.zip = true
dist.archives.tarGz = true
dist.archives.tarBz2 = true
